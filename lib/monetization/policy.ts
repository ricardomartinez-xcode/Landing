export type PublicMonetizationSurface='public_landing'|'public_docs'; export type MonetizationFormat='direct_sponsor'|'contextual_ad'; export type ProviderKind='direct'|'third_party'; export type ConsentState='granted'|'denied'|'unknown'|'not_required';
const forbidden=new Set(['ssh_terminal','auth','pairing','recovery','internal_console','remote_chrome_extension','private_chat','email_message']); const policy:Record<PublicMonetizationSurface,ReadonlySet<MonetizationFormat>>={public_landing:new Set(['direct_sponsor','contextual_ad']),public_docs:new Set(['direct_sponsor','contextual_ad'])};
export function evaluateMonetization(input:{surface:string;format:MonetizationFormat}){if(forbidden.has(input.surface))return{allowed:false,reason:'forbidden_surface' as const};const p=policy[input.surface as PublicMonetizationSurface];if(!p)return{allowed:false,reason:'unknown_surface' as const};return p.has(input.format)?{allowed:true,reason:'explicit_policy' as const}:{allowed:false,reason:'format_not_allowed' as const}}
export function canLoadProvider(input:{surface:string;provider:ProviderKind;consent:ConsentState}){const format:MonetizationFormat=input.provider==='direct'?'direct_sponsor':'contextual_ad';if(!evaluateMonetization({surface:input.surface,format}).allowed)return false;return input.provider==='direct'||input.consent==='granted'}
export function monetizationEnabled(env:Record<string,string|undefined>=process.env){return env.RELEAD_MONETIZATION_ENABLED==='true'}
